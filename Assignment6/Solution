docker network create my_network
docker run -d --name redis-network --network=my_network redis
docker run -d --name redis-go-app --network=my_network -p 8081:8080 -e REDIS_HOST=redis-network go-app-redis
check endpoint: http://localhost:8081/vote

NOTE: go-app-redis image created in assignment 5

